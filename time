		String dateWithoutTime = date.substring(0, date.indexOf("T"));
		LocalDate localDate = LocalDate.parse(dateWithoutTime);
		LocalDate curDate = LocalDate.now();
		if(curDate.getDayOfMonth() < localDate.getDayOfMonth()) {
			var res = "yes";
		} else {
			var res= "nope";
		}
